const uuid = require("uuid/v1"); 
const express = require("express");
const { exec  } = require('child_process');
let app = express();
let startBuild = (ssid, password, pin) => {
        
};
app.listen(process.env.PORT);

